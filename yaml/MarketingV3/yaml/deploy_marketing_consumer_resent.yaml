apiVersion: apps/v1
kind: Deployment
metadata:
 name: marketing-consumer-resent-deployment
 labels:
   app: marketing-consumer-resent
spec:
 replicas: 1
 selector:
   matchLabels:
     app: marketing-consumer-resent
 template:
   metadata:
     labels:
       app: marketing-consumer-resent
   spec:
     containers:
       - name: marketingv3
         image: {image}
         command: ["/bin/sh", "-c"]
         args: ["cd $MARKETING_HOME; sh prepare_env.sh && python3.5 -u mkt_consumer_resent.py"]
         envFrom:
           - configMapRef:
               name: mobio-config
         volumeMounts:
           - name: mobio-shared-data
             mountPath: /media/data/resources/
     imagePullSecrets:
       - name: registrypullsecret
     volumes:
       - name: mobio-shared-data
         persistentVolumeClaim:
           claimName: mobio-resources-pvc