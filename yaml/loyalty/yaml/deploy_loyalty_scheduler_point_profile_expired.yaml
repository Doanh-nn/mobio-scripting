apiVersion: apps/v1
kind: Deployment
metadata:
 name: loyalty-scheduler-point-profile-expired-deployment
 labels:
   app: loyalty-scheduler-point-profile-expired
spec:
 replicas: 1
 selector:
   matchLabels:
     app: loyalty-scheduler-point-profile-expired
 template:
   metadata:
     labels:
       app: loyalty-scheduler-point-profile-expired
   spec:
     containers:
       - name: loyalty
         image: {image}
         imagePullPolicy: Always
         command: ["/bin/sh", "-c"]
         args: ["cd $LOYALTY_HOME; sh prepare_env.sh && python3.5 -u loyalty_schedule_service.py loyalty_point_profile_expired"]
         envFrom:
           - configMapRef:
               name: mobio-config
         volumeMounts:
           - name: mobio-shared-data
             mountPath: /media/data/resources/
     imagePullSecrets:
       - name: registrypullsecret
     volumes:
       - name: mobio-shared-data
         persistentVolumeClaim:
           claimName: mobio-resources-pvc