apiVersion: apps/v1
kind: Deployment
metadata:
  name: profile-factory-schedule-scheduler-run-need-more-lal-deployment
  labels:
    app: profile-factory-schedule-scheduler-run-need-more-lal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: profile-factory-schedule-scheduler-run-need-more-lal
  template:
    metadata:
      labels:
        app: profile-factory-schedule-scheduler-run-need-more-lal
    spec:
      containers:
        - name: profile-factory
          image: {image}
          command: ["/bin/sh", "-c"]
          args: ["cd $FACTORY_HOME; sh prepare_env.sh && PYTHONPATH=./ python3.5 -u run_scheduler/factory_scheduler_scheduler_run_need_more_lal.py"]
          envFrom:
            - configMapRef:
                name: mobio-config
            - secretRef:
               name: mobio-secret
          resources:
            requests:
              memory: 70Mi
              cpu: 80m
            limits:
              memory: 1Gi
              cpu: 500m
          volumeMounts:
            - name: mobio-shared-data
              mountPath: /media/data/resources/
      imagePullSecrets:
        - name: registrypullsecret
      volumes:
        - name: mobio-shared-data
          persistentVolumeClaim:
            claimName: mobio-resources-pvc
    