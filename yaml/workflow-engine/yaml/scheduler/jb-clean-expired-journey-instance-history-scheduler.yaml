apiVersion: apps/v1
kind: Deployment
metadata:
  name: journey-scheduler-jb-clean-expired-journey-instance-history-deployment
  labels:
    app: journey-scheduler-jb-clean-expired-journey-instance-history
spec:
  replicas: 1
  selector:
    matchLabels:
      app: journey-scheduler-jb-clean-expired-journey-instance-history
  template:
    metadata:
      labels:
        app: journey-scheduler-jb-clean-expired-journey-instance-history
    spec:
      containers:
        - name: journey
          imagePullPolicy: Always
          image: {image}
          command: ["/bin/sh", "-c"]
          args: ["cd $JOURNEY_HOME; sh prepare_env.sh && python3.8 -u jb_job_services.py jb-clean-expired-journey-instance-history"]
          envFrom:
            - configMapRef:
                name: mobio-config
          volumeMounts:
            - name: mobio-shared-data
              mountPath: /media/data/resources/
      imagePullSecrets:
        - name: registrypullsecret
      volumes:
        - name: mobio-shared-data
          persistentVolumeClaim:
            claimName: mobio-resources-pvc
    