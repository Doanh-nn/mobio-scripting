apiVersion: apps/v1
kind: Deployment
metadata:
  name: survey-delete-draft-deployment
  labels:
    app: survey-delete-draft-consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: survey-delete-draft-consumer
  template:
    metadata:
      labels:
        app: survey-delete-draft-consumer
    spec:
      containers:
        - name: survey
          image: {image}
          command: [ "/bin/sh", "-c" ]
          args: [ "cd $SURVEY_FOLDER_NAME; sh prepare_env.sh && python3.8 -u start_delete_draft_survey.py" ]
          envFrom:
            - configMapRef:
                name: mobio-config
          volumeMounts:
            - name: mobio-shared-data
              mountPath: /media/data/resources/
            - name: mobio-public-shared-data
              mountPath: /media/data/public_resources/
      imagePullSecrets:
        - name: registrypullsecret
      volumes:
        - name: mobio-shared-data
          persistentVolumeClaim:
            claimName: mobio-resources-pvc
        - name: mobio-public-shared-data
          persistentVolumeClaim:
            claimName: mobio-public-resources-pvc