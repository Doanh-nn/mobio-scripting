apiVersion: apps/v1
kind: Deployment
metadata:
 name: social-consumer-reassign-conversation-merchant-deploy
 labels:
   app: social-consumer-reassign-conversation-merchant
spec:
 replicas: 1
 selector:
   matchLabels:
     app: social-consumer-reassign-conversation-merchant
 template:
   metadata:
     labels:
       app: social-consumer-reassign-conversation-merchant
   spec:
     containers:
       - name: social
         image: {image}
         imagePullPolicy: Always
         command: ["/bin/sh", "-c"]
         args: ["cd $SOCIAL_HOME; sh prepare_env.sh && python3.5 -u social_reassign_staff_each_merchant_v2_consumer.py"]
         resources:
           requests:
             memory: 70Mi
             cpu: 80m
           limits:
             memory: 1Gi
             cpu: 500m
         envFrom:
           - configMapRef:
               name: mobio-config
         volumeMounts:
           - name: mobio-shared-data
             mountPath: /media/data/resources/
     imagePullSecrets:
       - name: registrypullsecret
     volumes:
       - name: mobio-shared-data
         persistentVolumeClaim:
           claimName: mobio-resources-pvc