apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticket-report-staff-ticket-performance-schedule-deployment
  labels:
    app: ticket-report-staff-ticket-performance-schedule
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ticket-report-staff-ticket-performance-schedule
  template:
    metadata:
      labels:
        app: ticket-report-staff-ticket-performance-schedule
    spec:
      containers:
        - name: ticket
          image: {image}
          command: ["/bin/sh", "-c"]
          args: ["cd $TICKET_HOME; sh prepare_env.sh && python3.8 -u start_schedulers.py report-staff-ticket-performance-schedule"]
          resources:
            requests:
              memory: 70Mi
              cpu: 80m
            limits:
              memory: 1Gi
              cpu: 500m
          envFrom:
            - configMapRef:
                name: mobio-config
            - secretRef:
                name: mobio-secret
          volumeMounts:
            - name: mobio-shared-data
              mountPath: /media/data/resources/
            - name: mobio-public-shared-data
              mountPath: /media/data/public_resources/
      imagePullSecrets:
        - name: registrypullsecret
      volumes:
        - name: mobio-shared-data
          persistentVolumeClaim:
            claimName: mobio-resources-pvc
        - name: mobio-public-shared-data
          persistentVolumeClaim:
            claimName: mobio-public-resources-pvc