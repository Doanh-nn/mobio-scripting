apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticket-schedule-wait-assign-over-threshold-deployment
  labels:
    app: ticket-schedule-wait-assign-over-threshold
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ticket-schedule-wait-assign-over-threshold
  template:
    metadata:
      labels:
        app: ticket-schedule-wait-assign-over-threshold
    spec:
      containers:
        - name: ticket
          image: {image}
          command: ["/bin/sh", "-c"]
          args: ["cd $TICKET_HOME; sh prepare_env.sh && python3.8 -u start_schedulers.py handle-ticket-wait-assign-over-threshold"]
          envFrom:
            - configMapRef:
                name: mobio-config
          volumeMounts:
            - name: mobio-shared-data
              mountPath: /media/data/resources/
            - name: mobio-public-shared-data
              mountPath: /media/data/public_resources/
      imagePullSecrets:
        - name: registrypullsecret
      volumes:
        - name: mobio-shared-data
          persistentVolumeClaim:
            claimName: mobio-resources-pvc
        - name: mobio-public-shared-data
          persistentVolumeClaim:
            claimName: mobio-public-resources-pvc